<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hosinsa.mapper.NoticeMapper">

	<sql id="criteria">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach item='type' collection="typeArr">
				<trim prefix="OR">
					<choose>
						<when test="type == 'T'.toString()">
							ntitle like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'C'.toString()">
							ncontent like '%'||#{keyword}||'%'
						</when>
						<when test="type == 'W'.toString()">
							id like '%'||#{keyword}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>

	<select id="read" resultType="com.hosinsa.domain.NoticeVO">
		select * from hosinsa_notice where
		nno = #{nno}
	</select>


	<select id="getList" resultType="com.hosinsa.domain.NoticeVO">
		select * from hosinsa_notice
		where nno > 0 order by nno desc
	</select>

	<select id="getListWithPaging"
		resultType="com.hosinsa.domain.NoticeVO">
  <![CDATA[
   select 
     nno, ntitle, ncontent, id, nwritedate, nupdatedate, readcount 
   from 
       (
       select /*+INDEX_DESC(hosinsa_notice pk_notice) */
         rownum rn, nno, ntitle, ncontent, id, nwritedate, nupdatedate, readcount 
       from 
         hosinsa_notice
      where
  ]]>
		<include refid="criteria"></include> 
			
		
		<![CDATA[
		 rownum <= #{pageNum} * #{amount}
      )
  where rn > (#{pageNum} -1) * #{amount} 
   ]]>
	</select>


	<insert id="insert">
		insert into hosinsa_notice(nno, ntitle, ncontent, id)
		values(seq_notice.nextval, #{ntitle}, #{ncontent}, #{id})
	</insert>


	<insert id="insertSelectKey">
		<selectKey keyProperty="nno" order="BEFORE"
			resultType="long">
			select seq_notice.nextval from dual
		</selectKey>
		insert into hosinsa_notice(nno, ntitle, ncontent, id)
		values(#{nno}, #{ntitle}, #{ncontent}, #{id})
	</insert>


	<delete id="delete">
		delete from hosinsa_notice where nno = #{nno}
	</delete>


	<update id="update">
		update hosinsa_notice
		set
		ntitle= #{ntitle},
		ncontent= #{ncontent},
		nupdatedate = sysdate
		where nno = #{nno}
	</update>


	<select id="getTotalCount" resultType="int">
		select count(*) from hosinsa_notice
		where
		<include refid="criteria"></include>

		nno > 0
	</select>
	
	<update id="readCount">  <!-- 추가 -->
    update hosinsa_notice
    set readcount = readcount +1
    where nno = #{nno}
</update>

	
</mapper>